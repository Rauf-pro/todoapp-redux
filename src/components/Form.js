import React from "react";

const Form = () => {
  return (
    <div>
      <form>
        <input className="new-todo" placeholder="Add Todo" />
      </form>
    </div>
  );
};

export default Form;
